

-- table for dept: id int primary key, name varchar(30)
-- table for role: id int primary key, title varchar(30), salary decimal, dept_id int to hold reference to dept
-- table for employees: id int primary key, firstname varchar(30) lastname varchar(30), role id for reference to role, manager id referneces managers
